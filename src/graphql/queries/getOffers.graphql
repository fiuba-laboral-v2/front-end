query GetOffers($updatedBeforeThan: DateTime) {
  getOffers(updatedBeforeThan: $updatedBeforeThan) {
    offers {
      uuid
      title
      hoursPerDay
      minimumSalary
      maximumSalary
      updatedAt
      company {
        uuid
        companyName
        logo
      }
    }
    shouldFetchMore
  }
}
