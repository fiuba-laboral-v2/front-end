query getCompanyProfileById ($id: ID!) {
    getCompanyProfileById(id: $id) {
        id
        cuit
        companyName
        slogan
        description
        logo
        website
        email
        phoneNumbers
        photos
    }
}
