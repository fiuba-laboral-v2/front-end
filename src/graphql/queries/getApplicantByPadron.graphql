query getApplicantByPadron ($padron: Int!) {
    getApplicantByPadron(padron: $padron) {
        name
        surname
        padron
        description
        capabilities {
            uuid
            description
        }
        careers {
            code
            description
            credits
            creditsCount
        }
    }
}
