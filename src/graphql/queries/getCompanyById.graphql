query getCompanyById ($id: ID!) {
    getCompanyById(id: $id) {
        id
        cuit
        companyName
        slogan
        description
        logo
        website
        email
        phoneNumbers
        photos
    }
}
