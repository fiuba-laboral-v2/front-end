query getCompanies($updatedBeforeThan: PaginatedInput) {
  getCompanies(updatedBeforeThan: $updatedBeforeThan) {
    shouldFetchMore
    results {
      uuid
      cuit
      companyName
      businessName
      logo
      approvalStatus
      updatedAt
    }
  }
}
