import React, { FunctionComponent } from "react";
import { TitleBar } from "./component";

export const TitleBarContainer: FunctionComponent = () => {
  return <TitleBar title={"Bolsa de trabajo FIUBA"} />;
};
